# SFT 训练项目依赖清单
# 安装命令: pip install -r requirements.txt

# 核心框架
torch>=2.1.0
transformers>=5.10.0,<6.0.0
peft>=0.8.0
datasets>=2.16.0
accelerate>=0.28.0

# 数据处理与配置
pyyaml>=6.0
numpy>=1.24.0
pandas>=2.0.0

# 可视化
matplotlib>=3.7.0

# 量化支持
bitsandbytes>=0.43.0
